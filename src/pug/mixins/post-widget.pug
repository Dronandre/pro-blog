mixin post-widget(title,info, img, link="#")
    .post-widget
        .post-widget_img
            a(href= link)
                img(src="./img/small-thumbs/" + img, alt= title)
        h4.post-widget_title
            a(href= link) #{title}               
        .post-widget_info #{info}